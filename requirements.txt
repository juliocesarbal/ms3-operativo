# MS3 - Fase 1 (fundacion + encomiendas + tracking). Dependencias livianas.
# IA (TensorFlow), blockchain (Web3.py), FCM se agregan en fases posteriores.
fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0
pydantic>=2.7
pydantic-settings>=2.3
python-jose[cryptography]>=3.3
psycopg[binary]>=3.2
httpx>=0.27
python-multipart>=0.0.9
# ML (Fase 3): supervisado (retraso) + no supervisado (K-Means zonas)
scikit-learn>=1.5
pandas>=2.2
joblib>=1.3
# IA (Fase 3): vision por computadora, MobileNetV2 transfer learning
tensorflow-cpu>=2.21
Pillow>=10.0
# Blockchain (CU-14): cliente Ethereum (Sepolia)
web3>=7.0,<8.0
# QR de la guia (se genera al crear la encomienda; descargable e imprimible)
qrcode>=7.4
# Push FCM (notificaciones): Firebase Admin SDK. Sin credenciales es no-op.
firebase-admin>=6.5
